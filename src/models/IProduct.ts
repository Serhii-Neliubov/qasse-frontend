export interface IProduct {
    id: number,
    title: string,
    avg_rating: null | number,
    description: string,
    total_reviews: number,
    brand_name: string,
    images_url: string,
    category: string,
    sell_price: string,
    discount_price: string
}